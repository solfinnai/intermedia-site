name: Assemble package-lock.json
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/assemble-lockfile.yml'
permissions:
  contents: write
jobs:
  assemble:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build package-lock.json
        run: python3 .github/scripts/assemble-lockfile.py
      - name: Commit package-lock.json and remove parts
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          git rm -rf lock-b64
          git rm -f .github/workflows/assemble-lockfile.yml .github/scripts/assemble-lockfile.py
          git commit -m "Add package-lock.json"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && exit 0
            sleep $((i * 4))
          done
          exit 1
